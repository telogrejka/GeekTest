@model GeekTest.Models.webpages_Roles

@{
    ViewBag.Title = "Удаление роли";
}

<h2>@ViewBag.Title</h2>

<h3>Вы действительно хотите удалить роль "@Html.DisplayFor(model => model.RoleName)"?</h3>
</br>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Удалить" /> |
        @Html.ActionLink("Назад", "Index")
    </p>
}
